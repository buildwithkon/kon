{
  "name": "@konxyz/dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "KON v2 central dashboard (my.kon.xyz). Organizers sign in via id.kon.xyz, see the apps they own, and create new <app>.kon.xyz subnames. Per-app editing lives at <app>.kon.xyz/admin (apps/runtime); this dashboard owns app provisioning + cross-app overview.",
  "type": "module",
  "scripts": {
    "dev": "vp dev",
    "build": "vp build",
    "preview": "vp preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@konxyz/account-sdk": "workspace:*",
    "@konxyz/runtime-core": "workspace:*",
    "@konxyz/schemas": "workspace:*",
    "@preact/signals": "^1.3.0",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "typescript": "^5.6.3",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
