{
  "name": "@konxyz/plugin-forum",
  "version": "1.0.0",
  "private": true,
  "description": "KON v2 Forum plugin — GUN.js + SEA chat. Identity is wallet-derived (passkey PRF or wallet-sig fallback) via the wallet SDK. Replaces the v1 XMTP-based Forum which was never functional.",
  "type": "module",
  "main": "src/index.tsx",
  "exports": {
    ".": "./src/index.tsx"
  },
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@konxyz/runtime-core": "workspace:*",
    "gun": "^0.2020.1240",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@konxyz/plugin-shared": "workspace:*",
    "@preact/preset-vite": "^2.9.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
