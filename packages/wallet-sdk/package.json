{
  "name": "@konxyz/wallet-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Thin postMessage SDK for KON apps to talk to the central wallet origin (default: id.kon.xyz, self-host: id.<your-domain>). Apps never touch passkey or Safe directly — they openSignIn() / signTx() and the wallet popup handles the rest.",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./protocol": "./src/protocol.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@konxyz/runtime-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
