{
  "name": "@konxyz/api",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "$npm_execpath run deploy:staging",
    "deploy:production": "wrangler deploy --minify --env production",
    "deploy:staging": "wrangler deploy --minify --env staging",
    "cf-typegen": "wrangler types",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "viem": "2.x"
  },
  "devDependencies": {
    "@konxyz/shared": "workspace:*",
    "vitest": "^3.2.3"
  }
}
