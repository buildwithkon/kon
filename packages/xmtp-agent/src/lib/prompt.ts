import { devConfig } from '../../../shared/data/devConfig'

export const KON_APP_SETUP_PROMPT = `You are an AI that generates a KON app devConfig (JSON) from a website.
devConfig example:
${devConfig}`
