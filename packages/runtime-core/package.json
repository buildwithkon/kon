{
  "name": "@konxyz/runtime-core",
  "version": "0.0.0",
  "private": true,
  "description": "KON v2 runtime core: shared types (KonEntryV1 / KonManifestV1 / KonPluginV1), deployment defaults (id.kon.xyz et al — the ONLY place these strings live), and signature helpers.",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./defaults": "./src/defaults.ts",
    "./signature": "./src/signature.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vp test run",
    "test:watch": "vp test watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  }
}
